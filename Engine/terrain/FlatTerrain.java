package terrain;

/**
 * @deprecated Bu sınıf terrain.flat.FlatTerrain'e taşındı.
 * Lütfen importları güncelleyin:
 *   import terrain.flat.FlatTerrain;
 *
 * Bu dosya geriye dönük uyumluluk için yönlendirme olarak bırakılmıştır.
 * Yeni kod terrain.flat.FlatTerrain kullanmalıdır.
 */
@Deprecated
public class FlatTerrain extends terrain.flat.FlatTerrain {
    public FlatTerrain(float width, float depth) {
        super(width, depth);
    }
}
